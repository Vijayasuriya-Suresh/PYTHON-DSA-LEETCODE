class Solution(object):
    def runningSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        runSum = []
        pev_number = 0
        for n in nums:
            number = number + n
            runSum.append(pev_number)

        return runSum